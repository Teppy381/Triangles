if (BUILD_TESTING)
    add_executable(my_test my_tests.cpp ../geometry.hpp ../float_cmp.hpp)
    target_link_libraries(my_test PRIVATE GTest::gtest_main)

    include(GoogleTest)
    gtest_discover_tests(my_test)
endif()
